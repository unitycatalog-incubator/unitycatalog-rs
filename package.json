{
  "name": "unitycatalog",
  "workspaces": [
    "node/client",
    "docs"
  ],
  "scripts": {
    "openapi": "prettier --write openapi/openapi.yaml",
    "lint-fix": "biome check --write && biome format --write ."
  },
  "dependencies": {
    "eslint": "^10.0.2",
    "prettier": "^3.8.1",
    "yaml": "^2.8.2"
  },
  "devDependencies": {
    "@biomejs/biome": "^2.2.0"
  }
}
