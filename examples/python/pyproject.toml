[project]
name = "unitycatalog-examples"
version = "0.0.1"
requires-python = ">=3.10"
dependencies = ["unitycatalog-client"]

[tool.uv.sources]
unitycatalog-client = { workspace = true }

[tool.pyright]
venvPath = "../.."
venv = ".venv"
